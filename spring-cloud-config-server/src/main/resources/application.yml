spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/damvuong/spring-cloud
          searchPaths: configuration
          default-label: part3/centralize_configuration